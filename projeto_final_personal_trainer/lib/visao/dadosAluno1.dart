import 'package:flutter/material.dart';

class DadosAluno1 extends StatelessWidget {
  const DadosAluno1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Dados do Aluno - Parte 1")),
      body: const Center(
        child: Text("Tela de dados do aluno - parte 1"),
      ),
    );
  }
}
